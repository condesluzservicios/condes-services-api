version: "3.9"
services:
  condes_server_api:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - .env
    # volumes:
    #   - .:/home/app
    #   - node_modules:/home/app/node_modules